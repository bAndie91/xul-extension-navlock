// NavLock default preferences
// Per-tab lock state is held in memory (not persisted across sessions).
// Only the global violation mode is stored here.
pref("extensions.navlock.mode", "redirect"); // "redirect" or "block"
